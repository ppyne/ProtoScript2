import Sys;
import Io;

function main() : void {
    list<string> args = [];
    list<byte> input = [];
    try {
        Sys.execute("/no/such/program", args, input, false, false);
    } catch (InvalidExecutableException e) {
        Io.printLine("InvalidExecutableException");
    }
}
