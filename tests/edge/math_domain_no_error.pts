import Io;
import Math;

function main() : void {
    float n = Math.sqrt(-1.0);
    bool okNaN = (n != n);
    float inf = Math.log(0.0);
    bool okInf = (inf < -1e308);
    bool ok = okNaN && okInf;
    Io.printLine(ok.toString());
}
