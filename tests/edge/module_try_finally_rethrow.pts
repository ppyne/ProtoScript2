import Io;

import test.throw.{fail};

function main() : void {
    try {
        try {
            fail();
        } finally {
            Io.printLine("cleanup");
        }
    } catch (Exception e) {
        Io.printLine("caught");
    }
}
