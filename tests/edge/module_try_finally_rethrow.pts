import Io;

import test.throw.{fail};

function main() : void {
    try {
        try {
            fail();
        } finally {
            Io.printLine("cleanup");
        }
    } catch (RuntimeException e) {
        Io.printLine("caught");
        Io.printLine(e.code);
    }
}
