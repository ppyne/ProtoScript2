import Io;

function main() : void {
    list<int> xs = [];
    map<string, int> mm = {};

    Io.printLine(xs.length().toString());
    Io.printLine(mm.length().toString());

    for (int v of xs) {
        Io.printLine("bad");
    }

    for (string k in mm) {
        Io.printLine("bad");
    }
}
