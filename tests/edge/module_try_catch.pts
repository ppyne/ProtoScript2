import Io;

import test.throw.{fail};

function main() : void {
    try {
        fail();
        Io.printLine("no");
    } catch (Exception e) {
        Io.printLine("caught");
    }
    Io.printLine("after");
}
