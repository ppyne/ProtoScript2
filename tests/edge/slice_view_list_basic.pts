import Io;

function main() : void {
    list<int> xs = [1, 2, 3, 4];
    slice<int> s = xs.slice(1, 2);
    s[0] = 9;
    view<int> v = xs.view(1, 2);
    bool ok = (xs[1] == 9) && (v[0] == 9) && (s.length() == 2) && !s.isEmpty();
    Io.printLine(ok.toString());
}
