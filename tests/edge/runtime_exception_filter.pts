import Io;

import test.throw.{fail};

function main() : void {
    try {
        fail();
    } catch (RuntimeException e) {
        Io.printLine("runtime");
        Io.printLine(e.code);
    } catch (Exception e) {
        Io.printLine("user");
    }
}
