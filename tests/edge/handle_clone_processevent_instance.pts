import Sys;

function main() : void {
    list<string> args = ["x"];
    list<byte> input = [];
    ProcessResult r = Sys.execute("/bin/echo", args, input, true, false);
    ProcessEvent e = r.events()[0];
    e.clone();
}
