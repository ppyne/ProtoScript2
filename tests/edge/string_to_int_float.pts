import Io;

function main() : void {
    string si = "42";
    string sf = "3.5";
    int i = si.toInt();
    float f = sf.toFloat();
    bool ok = (i == 42) && (f.toInt() == 3);
    Io.printLine(ok.toString());
}
