import RegExp;
import Io;

function main() : void {
    RegExp email = RegExp.compile("^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\\.[A-Za-z]{2,}$", "");
    Io.printLine(email.test("john.doe@example.com", 0));
    Io.printLine(email.test("bad@@example", 0));
}
