import Io;

function main() : void {
    CivilDateTime a = CivilDateTime.clone();
    CivilDateTime b = CivilDateTime.clone();
    b.setYear(2000);
    a.setYear(2026);
    Io.printLine((a.year() != b.year()).toString());
}
