import Io;

function main() : void {
    try {
        Exception e = Exception.clone();
        e.message = "Quelque chose s'est mal passe";
        throw e;
    } catch (Exception e) {
        Io.printLine(e.message);
    }
}
