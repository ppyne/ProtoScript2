import Io;

function main() : void {
    list<string> parts = ["hello", " ", "world", Io.EOL];
    string a = parts.join("");
    string b = parts.concat();
    bool ok = (a == b) && a.startsWith("hello world") && a.endsWith(Io.EOL);
    Io.printLine(ok.toString());
}
