prototype A {
  function clone() : A {
    return super.clone();
  }
}

prototype B : A {
  function clone() : B {
    return super.clone();
  }
}

prototype C : B { }

function main() : void {
  C c = C.clone();
}
