import Fs;
import Io;

function main() : void {
    string base = Io.tempPath();
    Fs.mkdir(base);
    TextFile f = Io.openText(base.concat("/x.txt"), "w");
    f.write("x");
    f.close();
    Walker w = Fs.walk(base, 2, false);
    PathEntry e = w.next();
    e.clone();
}
