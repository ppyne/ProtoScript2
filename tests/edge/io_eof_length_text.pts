import Io;

function main() : void {
    var f = Io.openText("io_eof.txt", "w");
    f.close();

    var r = Io.openText("io_eof.txt", "r");
    var chunk = r.read(1);
    r.close();

    bool ok = (chunk.length() == 0);
    Io.printLine(ok.toString());
}
