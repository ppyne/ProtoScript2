import Io;
import RegExp;

function main() : void {
    RegExp r = RegExp.compile(",", "");
    list<string> parts = r.split("a,b,c", 0, 10);
    Io.print(parts.length().toString().concat("|").concat(parts[1]));
}
