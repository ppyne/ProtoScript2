import Io;
import RegExp;

function main() : void {
    RegExp r = RegExp.compile("é", "");
    RegExpMatch m = r.find("aéb", 0);
    Io.print(m.start.toString().concat("|").concat(m.end.toString()).concat("|").concat(m.groups[0]));
}
